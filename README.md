# nexusnode-gateway
NexusNode is a high-performance LLM gateway designed to run globally on the edge via Cloudflare Workers. It provides a single, unified endpoint for your applications while handling the complexity of multiple LLM providers (e.g., OpenAI, Anthropic), automatic failover, and observability.
